---
dotfiles_repo: "https://github.com/ceso/dotfiles.git"
dotfiles_repo_version: master
dotfiles_repo_accept_hostkey: true

# Expand ~ to the actual home directory
dotfiles_repo_local_destination: "{{ ansible_env.HOME }}/Projects/repos/dotfiles"
dotfiles_home: "{{ ansible_env.HOME }}"

# Homebrew prefix, used to add brew binaries to PATH when running GNUmakefiles
dotfiles_brew_prefix: "/home/linuxbrew/.linuxbrew"

# Entries to symlink into ~/
dotfiles_files:
  - .config/bat
  - .config/Code
  - .config/eza
  - .config/fish
  - .config/ghostty
  - .config/git
  - .config/nvim
  - .config/zellij
  - .homebrew
  - .ssh

# Directory whose contents are individually symlinked to ~/.local/bin/
dotfiles_bin_dir: bin

# eof
